{% extends 'base.html.twig' %}

{% block title "Hello City"%}

{% block body %}
    <img src="{{ asset('img/france.png') }}" alt="French Flag" width=250 class="rounded shadow-md mt-12 h-30" />
    <h1 class="mt-5 text-3xl sm:text-5xl font-semibold text-indigo-600">Hello from France!</h1>

    <p class="mt-5 text-lg text-gray-800">It's currently {{ 'now'|date('H:i') }}.</p>


{% endblock %}